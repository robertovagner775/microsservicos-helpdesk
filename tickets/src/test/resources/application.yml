spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:suporteTest
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  mvc:
    problemdetails:
      enabled: true
server:
  error:
    include-stacktrace: never